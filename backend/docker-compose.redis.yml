services:
  redis:
    image: redis:7-alpine
    container_name: ai-knowledge-hub-redis
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes
    volumes:
      - ai-knowledge-hub-redis-data:/data

volumes:
  ai-knowledge-hub-redis-data:
